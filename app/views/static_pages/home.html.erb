<% if signed_in? %>
	<div class="row">
		<aside class="span12">
			<section>
				<%= render 'shared/user_info' %>
			</section>
			<section>
				<%= render 'shared/hourlog_form' %>
			</section>
	</div>
<% else %>
	<div class="center hero-unit">
	  <h1>Medical Student Duty Hour Logger</h1>

	  <h2>

	  </h2>

	  <%= link_to "Sign up now", signup_path, class: "btn btn-large btn-primary" %>
	</div>
<% end %>
